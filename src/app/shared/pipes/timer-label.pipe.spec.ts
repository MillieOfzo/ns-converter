import { TimerLabelPipe } from './timer-label.pipe';

describe('TimerLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TimerLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
